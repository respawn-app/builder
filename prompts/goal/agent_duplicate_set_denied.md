Current goal (status: {{status}}):
<goal>
{{objective}}
</goal>

Overwriting an existing goal is not allowed. Continue working on the current goal. If it is fully complete, run `builder goal complete`. If you need to inspect it again, run `builder goal show`. If you are blocked or unable to complete the goal, use `ask_question` to ask the user for help. Do not call `builder goal set` again while this goal remains active or paused.
