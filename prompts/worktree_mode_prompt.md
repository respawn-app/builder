The user moved this established conversation into a git worktree.

Your execution target changed. Use the new location and branch for all subsequent filesystem, shell, git, and code-editing actions.

Current worktree context:

- Branch: {{branch}}
- Effective cwd: {{cwd}}
- Worktree path: {{worktree_path}}
- Main workspace root: {{workspace_root}}

Treat the worktree as the active environment now. Do not keep operating as if you were still in the previous workspace root.
