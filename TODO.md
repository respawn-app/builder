# TODO

- Evaluate optional runtime mode that ignores model-selected channels entirely and derives `commentary`/`final` deterministically from step state (deferred until current protocol fixes are validated).
